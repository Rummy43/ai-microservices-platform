apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-service
  namespace: microservices
spec:
  selector:
    matchLabels:
      app: ai-service
  endpoints:
  - port: http
    path: /actuator/prometheus
    interval: 30s
